---
import { getPostUrlBySlug } from "@/utils/getPostPaths";
import { toTransitionName } from "@/utils/toTransitionName";
import Datetime from "./Datetime.astro";
import type { PostSummary } from "@/utils/blogTypes";

type Props = {
  variant?: "h2" | "h3";
  post: PostSummary;
};

const { variant: Heading = "h2", post } = Astro.props;
const { title, description, slug, publishedAt, updatedAt } = post;
---

<li class="my-6">
  <a
    href={getPostUrlBySlug(slug, Astro.currentLocale)}
    class:list={[
      "text-accent inline-block text-lg font-medium",
      "decoration-dashed underline-offset-4 hover:underline",
      "focus-visible:no-underline focus-visible:underline-offset-0",
    ]}
  >
    <Heading transition:name={toTransitionName(title)}>
      {title}
    </Heading>
  </a>
  <Datetime publishedAt={publishedAt ?? ""} updatedAt={updatedAt} />
  <p>{description}</p>
</li>
